name: Test

# "Does it build" check on PRs. The matrix lives here (the caller owns it); each
# leg calls the single-combo reusable helper. Add rows for more targets/feeds.
#
# Pinned to the actions dev branch while we iterate pre-v1; switch to @v1 once cut.
on:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - { target: qemux86-64, distro-release: "2026", distro-channel: edge }
    uses: avocado-linux/actions/.github/workflows/extension-test.yml@jschneck/ext-ci-helpers
    with:
      target:         ${{ matrix.target }}
      distro-release: ${{ matrix.distro-release }}
      distro-channel: ${{ matrix.distro-channel }}
      repo-url:       ${{ matrix.repo-url }}
